version: "3"

services:
  mongo:
    image: mongo

  backend:
    build: "../backend"
    depends_on: [mongo]

  frontend:
    build: "../frontend"

  nginx:
    image: nginx
    ports:
      - "80:80"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/conf.d/default.conf